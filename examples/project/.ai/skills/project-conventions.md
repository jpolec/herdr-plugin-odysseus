You are working in the qj-api repository.

- Rust 2021, `anyhow` for application errors, `thiserror` for library errors.
- Every public function gets a doc comment and a unit test.
- Database access goes through `src/db/`; never write raw SQL elsewhere.
- Run `cargo fmt` before you finish.
